<section class="py-12">
  <div class="relative overflow-hidden rounded-[2.5rem] border border-border bg-surface px-8 py-12 shadow-sm sm:px-16 md:py-20">
    <div class="absolute -right-24 -top-24 h-96 w-96 rounded-full bg-primary/5 blur-3xl"></div>
    <div class="absolute -left-24 -bottom-24 h-96 w-96 rounded-full bg-primary/5 blur-3xl"></div>

    <div class="relative mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-text sm:text-4xl">
        Receba novidades toda <span class="text-primary">semana</span>
      </h2>
      <p class="mt-4 text-lg leading-8 text-text-muted">
        Entre na comunidade do Caf&eacute; Com F&eacute;. Conteudos sobre programacao, carreira,
        rotina com cafe e vida com Deus.
      </p>

      <form class="mt-10 flex flex-col gap-y-4 sm:flex-row sm:gap-x-4">
        <label for="email-address" class="sr-only">Endereco de e-mail</label>
        <input
          id="email-address"
          name="email"
          type="email"
          required
          placeholder="Seu melhor e-mail"
          class="min-w-0 flex-auto rounded-full border border-border bg-background px-6 py-4 text-text shadow-sm focus:ring-2 focus:ring-primary focus:outline-none sm:text-sm sm:leading-6"
        />
        <button
          type="submit"
          class="flex-none rounded-full bg-primary px-8 py-4 text-sm font-bold text-white shadow-lg hover:bg-primary-dark transition-all transform hover:scale-105 active:scale-95 cursor-pointer"
        >
          Inscrever-se
        </button>
      </form>

      <p class="mt-4 text-xs text-text-muted/60">
        Sem spam. Voce pode sair quando quiser.
      </p>
    </div>
  </div>
</section>
